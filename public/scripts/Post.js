'use strict';

var React = require('react');
var ReactDOM = require('react-dom');

var PostContainer = require('./PostContainer');

ReactDOM.render(<PostContainer />, document.getElementById('post-container'));
